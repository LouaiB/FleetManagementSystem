@{
    ViewData["Title"] = "Log In";
}
<style>
    html, body, h1, h2, h3, h4, h5 {
        font-family: "Open Sans", sans-serif
    }

    body {
        background: #ccc
    }

    .box {
        text-align: left;
        position: relative;
        top: 40px;
        padding: 10px 30px;
    }

    .box {
        width: 40%;
       padding-bottom:40px;
        background: #FFF;
        margin: 40px auto;
    }

    .effect6 {
        position: relative;
        -webkit-box-shadow: 0 1px 4px rgba(0, 0, 0, 0.3), 0 0 40px rgba(0, 0, 0, 0.1) inset;
        -moz-box-shadow: 0 1px 4px rgba(0, 0, 0, 0.3), 0 0 40px rgba(0, 0, 0, 0.1) inset;
        box-shadow: 0 1px 40px rgba(0, 0, 0, 0.3), 0 0 40px rgba(0, 0, 0, 0.1) inset;
    }

        .effect6:before, .effect6:after {
            content: "";
            position: absolute;
            z-index: -1;
            -webkit-box-shadow: 0 0 20px rgba(0,0,0,0.8);
            -moz-box-shadow: 0 0 20px rgba(0,0,0,0.8);
            box-shadow: 0 0 50px rgba(0,0,0,0.8);
            top: 50%;
            bottom: 0;
            left: 10px;
            right: 10px;
            -moz-border-radius: 100px / 10px;
            border-radius: 100px / 10px;
        }

        .effect6:after {
            right: 10px;
            left: auto;
            -webkit-transform: skew(8deg) rotate(3deg);
            -moz-transform: skew(8deg) rotate(3deg);
            -ms-transform: skew(8deg) rotate(3deg);
            -o-transform: skew(8deg) rotate(3deg);
            transform: skew(8deg) rotate(3deg);
        }
</style>
<br /><br />
<div class="box effect6">
    <h3>Log In</h3>
    
    <div class="w3-container w3-padding">
        <form asp-controller="Managers" asp-action="LogIn">
            <!--
    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
    <div asp-validation-summary="All" class="text-danger"></div>
    -->
            <h6 class="w3-opacity">Email :</h6>
            <input type="email" name="Email" style="width:100%" class="w3-border w3-padding" 
                   value="abdullah@gmail.com">

            <h6 class="w3-opacity">Password</h6>
            <input type="password" name="Password" style="width:100%" class="w3-border w3-padding" 
                   value="Aa12345$">
            <center><span class="text-danger">@ViewData["Error"]<br /></span></center>
            <br />
        
            <button type="submit" class="w3-button w3-theme w3-right">
                Log In
                <!-- <i class='fas fa-angle-double-right'></i> -->
            </button>
        </form>
    </div>
</div>



@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
